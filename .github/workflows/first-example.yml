name : My first workflow
on: [push]
jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - name: My first step
        run: echo "Hello, world! This is my first GitHub Actions workflow using branch main"
      - name: My second step
        run: pwd